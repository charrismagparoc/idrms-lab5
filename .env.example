# Supabase (required)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Semaphore SMS API (optional — for real SMS alerts)
VITE_SEMAPHORE_KEY=your-semaphore-api-key

# OpenWeatherMap (optional — falls back to static data)
# Key is currently hardcoded in useWeather.js, replace as needed
